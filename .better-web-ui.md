## Design Context

### Users
系统主要面向制造业现场的运维主管、维修工程师、仓库管理员、报修人与系统管理员。用户在桌面浏览器中高频处理派单、维修、SLA 异常和库存业务，需要快速判断状态、明确责任并避免误操作。

### Brand Personality
严谨、可靠、克制。语言保持正式且直接，让用户感到系统可信、状态透明、操作后果明确。

### Aesthetic Direction
延续现有浅色工业管理界面：深蓝侧栏、蓝灰中性色、白色业务表面、轻量边框和阴影、轻微圆角。优先支持 1366×768 与 1920×1080 桌面视口，不引入与既有系统冲突的强装饰、渐变文字或霓虹效果。

### Implementation Defaults
沿用现有 Vue 3、Vite、Vue Router、Pinia、Axios、Element Plus 与全局 CSS 变量；组件采用 Composition API 和 `<script setup lang="ts">`。表格、表单、对话框和消息反馈继续使用 Element Plus，不新增表单、数据表或虚拟列表依赖。实现选择顺序为：现有项目栈、用户明确要求、框架兜底。

### Design Principles
- 先用间距、字号和字重建立层级，再使用克制的语义色强调 SLA 风险与状态。
- 每个操作面只保留一个明确主动作，推荐信息用于辅助判断，不替代主管决策。
- 评分、截止时间和通知来源必须可解释，避免只展示结论而隐藏依据。
- 高密度业务信息采用清晰分组与可扫描表格，不堆叠无意义卡片。
- 所有关键状态兼顾文字与颜色，重要操作提供加载、空状态和错误反馈。
