# 1 最终开发基线

- 开发前 HEAD：`0ea8666 docs: record stage 11 statistics verification`
- 第11阶段功能 Commit：`0044160 feat: implement KPI statistics and ECharts dashboard`
- 开发前测试基线：277 项通过，0 失败、0 错误、0 跳过；Playwright 2/2。
- Git 状态：开发前干净；日期 2026-09-18；版本为第11阶段验证基线。

# 2 任务书完成矩阵

完整逐项矩阵见《任务书功能完成矩阵》。结论：用户管理、PDF、运行小时、审计、备份恢复及设备/工单附件闭环均已补齐。

| 任务书要求 | 实现位置 | 测试 | 最终状态 |
|---|---|---|---|
| 八大业务与支撑模块 | backend/frontend 对应模块 | Maven + MySQL/Redis + Chrome | 已完成 |
| 六项 KPI | statistics 模块 | 单元、直接 SQL、浏览器 | 已完成 |
| PDF、Excel、Actuator、审计、备份 | workorder/system/audit/scripts | 单元、运行态、恢复库 | 已完成 |
| 设备与工单附件 | 通用存储 + equipment/workorder | 单元、真实库、Chrome 上传/回显/下载 | 已完成 |

# 3 最终系统功能清单

认证与 RBAC、组织管理、设备台账和运行小时、设备/工单附件、故障报修、维修/PM 工单、智能派单、SLA、备件库存、知识审核、TF-IDF 相似推荐、六项 KPI、ECharts、消息、AOP 审计、PDF、Excel、Actuator、MySQL 备份恢复。

# 4 最终技术栈

JDK 17、Spring Boot 3.5.16、Spring Security、Validation、AOP、Actuator、MyBatis-Plus 3.5.17、MySQL 8、Redis 8、OpenPDF 1.3.43；Vue 3.5.42、Vite 7.3.6、Pinia、Vue Router、Axios、Element Plus、ECharts、Playwright。

# 5 最终角色与权限

| 角色 | 核心权限 | 数据范围 |
|---|---|---|
| 管理员 | 系统与业务全量 | 全部 |
| 运维主管 | 派单、验收、SLA、知识审核、统计 | 主车间及所属班组 |
| 工程师 | 本人/班组工单、维修、相似推荐 | 本人及所属班组 |
| 仓库管理员 | 仓库、库存、流水 | 授权仓库 |
| 报修人 | 报修及本人关联工单 | 本人报修数据 |

# 6 核心业务闭环

设备 RUNNING → 报修 → 自动生成维修工单并置 FAULT → Top3 推荐 → 主管派单 → 工程师维修/领料 → 提交验收 → 通过后 COMPLETED/设备 RUNNING → 知识草稿与审核 → 业务事实进入 KPI 聚合。

# 7 最终状态机

- 设备：RUNNING、FAULT、REPAIRING、STOPPED、SCRAPPED，Service 校验合法转换并写履历。
- 工单：待派单、待接单、执行中、挂起、待验收、完成、取消、无法修复；非法跳转拒绝。
- PM：计划到期幂等生成 MAINTENANCE 工单，经统一派单、逐项执行和验收；不把设备误置为 FAULT。

# 8 数据库最终状态

- 表数量：39。
- SQL 初始化：新库 `industrial_maintenance_init_test_20260918_b` 成功，5 角色、6 演示用户、69 个非空权限码。
- 增量 SQL：01—25 用途与顺序已在 `sql/README.md` 固化。
- 空库初始化结果：39 表，Stage12 五项权限存在。

# 9 任务书补缺情况

- PDF：已实现中文嵌入字体工单 PDF 及页面下载。
- Excel：设备原有导入导出；新增用户模板与批量导入。
- Actuator：仅公开 health/info；health 实测 200/UP。
- 备份：PowerShell dump/restore 脚本真实执行。
- 审计：写操作 AOP 落库，敏感参数值不记录；分页 API 可查。
- 消息：已有 SLA 站内消息、已读/全部已读。
- 其他：新增运行小时人工登记和用户完整 CRUD。
- 附件：设备和工单均已实现本地目录存储、数据库元数据、10 MB 上限、扩展名白名单、上传/列表/下载与页面回显。

# 10 自动化测试

- 开发前：277/277 通过，0 失败、0 错误、0 跳过。
- 中间回归：新增审计与用户服务曾造成 10 个无数据源上下文错误，修复后 286/286 通过。
- 附件接入后的中间回归暴露无数据源测试上下文强制 Mapper 注入问题，修复后相关 10/10 通过。
- 最终：292/292 通过，0 失败、0 错误、0 跳过；阶段 4—12 的真实数据库开关全部启用，总耗时 55.777 秒。

# 11 功能测试

正式用例 24 条全部通过。详细前置条件、步骤和实际结果见《最终测试报告》；后端自动化测试为 292/292 通过。

# 12 权限安全测试

- 未登录：受保护接口实测 HTTP 401。
- Token：错误、过期、刷新轮换、退出黑名单由认证集成测试覆盖。
- RBAC：按钮与 `@PreAuthorize` 双层控制。
- 数据权限：工单、设备、统计经 Scope Service；直接 IDOR 测试覆盖。
- 仓库越权：按授权仓库拒绝。
- 工单越权：工程师/报修人仅访问本人或授权范围。
- 附件读取复用设备/工单查看权限，设备上传要求 `equipment:update`，工单上传要求 `workorder:process` 或 `workorder:accept`；业务对象的数据范围校验在 Service 中继续执行。
- 结果：现有自动化与真实联调范围通过。

# 13 基本性能测试

- 环境：Windows 开发电脑，本机 MySQL/Redis/JAR；PowerShell 脚本。
- 接口：当前用户、设备、工单、库存、KPI、操作日志。
- 并发：5；请求数：每接口 20，共 120。
- 平均：35.99—81.32 ms；P95：63.99—150.28 ms；最大 161.97 ms；错误率 0%。
- 真实结论：足以支持当前答辩规模交互，不外推企业容量；登录因一次性验证码未并发压测。

# 14 算法实验

- 选择算法：智能派单加权评分；TF-IDF + 余弦相似度。
- Baseline：派单实验见《智能派单算法实验》，相似推荐以文本相似排序和空语料边界为对照。
- 数据规模：项目演示/测试数据，不声称工业大样本。
- 指标：Top3 命中与排序解释、余弦分数与 Top5 稳定性。
- 真实结果：自动化与浏览器用例通过；主管保留人工决策权。

# 15 数据备份与恢复

- 备份命令：`scripts/backup-mysql.ps1`，密码仅通过环境/安全输入传递。
- 备份文件：`backups/industrial_maintenance_20260918_091156.sql`，652030 字节（目录被 Git 忽略）。
- 恢复数据库：`industrial_maintenance_restore_final_20260918`。
- 恢复结果：39 表、5 角色、487 台设备、5 项 Stage12 权限。

# 16 Actuator健康检查

- 接口：`GET /actuator/health`。
- 结果：HTTP 200，`{"status":"UP"}`；部署验证时启用 DB/Redis health，未开放敏感 Actuator 端点。

# 17 部署验证

- 环境：Windows 本机，JDK 17、MySQL 3306、Redis 6379。
- 数据库：开发库及独立初始化/恢复库均可用。
- Redis：`PONG`。
- 后端：正式 repackaged JAR 在 8080 启动。
- 前端：Vite 开发联调与 production build 均成功。
- 登录：真实验证码、Redis 取值、BCrypt 演示账号登录成功。
- Smoke Test：`/auth/me`、设备、工单、操作日志 200；无 Token 401。
- 最终结果：当前机器部署通过；未在第二台全新主机验证。

# 18 浏览器最终验收

- 1366×768：派单、SLA、知识、KPI、用户管理、运行小时、设备/工单附件和五角色登录通过。
- 1920×1080：派单/SLA、知识/相似推荐、统计页无横向溢出。
- Console：最终 8/8 Playwright 通过批次无阻断错误。
- 页面异常：消息已读旧标签时序问题已采用乐观更新修复；最终全套复跑通过。

# 19 五角色验收

管理员、主管、工程师、仓库管理员、报修人的鉴权与数据范围均由自动化测试覆盖；最终 Playwright 逐一通过真实登录表单、Redis 验证码和开发演示密码进入工作台，形成五角色浏览器登录记录。

# 20 核心维修业务验收

真实 MySQL 集成测试分别执行报修建单、设备状态联动、派单、接单、维修记录、备件领退、提交验收、退回/通过、设备恢复和知识沉淀。它们共享真实业务表与事务；本轮没有用单一新工单在浏览器连续走完所有角色步骤。

# 21 PM业务验收

Stage8 真实库测试覆盖计划创建、到期扫描、重复触发幂等、MAINTENANCE 工单、检查项、执行和验收履历；PM 不触发故障状态。当前轮复跑包含 Stage8 开关。

# 22 库存业务验收

Stage7 真实库测试覆盖入库、出库、工单领用、退库、调拨、盘点、报废和预警；库存、业务记录、流水在事务内一致，库存不足与越权均拒绝。

# 23 KPI最终核验

固定 Stage11 数据集由测试直接插入并以 API Service 对照手工公式：

| KPI | 手工计算 | API结果 | 页面结果 | 是否一致 |
|---|---:|---:|---:|---|
| MTBF | 60/2=30.00h | 30.00 | 同 API | 是 |
| MTTR | (2+4)/2=3.00h | 3.00 | 同 API | 是 |
| 按时关单率 | 1/2=50.00% | 50.00% | 同 API | 是 |
| 一次修复率 | 2/2=100.00% | 100.00% | 同 API | 是 |
| 可用率 | 60h/72h=83.33% | 83.33% | 同 API | 是 |
| 备件周转率 | 20/90=0.22 | 0.22 | 同 API | 是 |

# 24 文档最终状态

需求、开发设计、系统设计、数据库、API、测试、部署和系统说明已同步第12阶段事实。本仓库已新增最终矩阵、测试报告、事实基线、截图映射和恢复清单；正式 V1.1 文档包 00—08 及第12阶段索引已更新附件、292/292、8/8 与未冻结原因。

# 25 论文素材

已有流程/状态机/ER 文档、Stage9—12 真实截图、功能测试表、算法实验、KPI 公式和性能数据。截图映射见《论文截图映射》；失败截图不得入论文。

# 26 答辩演示准备

- 主线：设备→报修→派单→维修/领料→验收→知识→KPI。
- 测试账号：五类本地演示账号；密码只用于开发演示。
- 演示数据：已有组织、设备、工单、库存、知识和统计数据。
- 预计系统演示时间：8—10 分钟。
- 恢复方案：见《答辩演示快速恢复清单》。

# 27 最终问题与风险

- P0：0（空库 SQL 阻断已修复）。
- P1：0。
- P2：操作日志无独立前端页；第二台机器未部署。
- P3：前端主包和统计包存在大于 500 kB 的非阻断构建警告。

# 28 系统明确不足

主要在本机部署；未接真实工业 IoT；运行小时人工维护；派单为规则评分、推荐为传统文本模型；演示数据规模有限；没有企业长期压力与可用性验证；附件为本地文件存储且未做病毒扫描或对象存储生命周期管理。

# 29 最终未完成项

1. 单一新维修工单由五角色在浏览器连续走完的最终录像式验收。
2. 第二台全新主机部署。
3. 生产化附件内容扫描、对象存储与生命周期管理（毕业设计范围外增强项）。

# 30 系统最终完成度

任务书功能项、构建、测试、数据库、备份恢复、本机部署、附件闭环和五角色登录证据均已完成。仍缺异机部署与单一工单跨角色录像式证据，真实评估为“已具备答辩演示与论文事实整理条件，但尚未满足自行设定的最终冻结全部证据条件”，不写 100%。

# 31 Git最终状态

最终功能 Commit：`4171223 feat: complete stage 12 acceptance gaps`。本报告及证据将由随后文档 Commit 固化；因第二台全新主机部署和单一工单跨角色录像式证据仍缺，未创建 `v1.0.0` Tag，避免把证据未齐状态标为正式冻结版。

# 32 最终结论

当前系统已达到论文事实整理和答辩演示条件，本机可启动、五角色可登录、任务书业务可操作、可从空库初始化并备份恢复，附件闭环及全量回归均通过。由于第二台全新主机部署和单一工单跨角色录像式验收尚无真实证据，本轮只提交已验证代码与文档，不创建最终冻结 Tag。
